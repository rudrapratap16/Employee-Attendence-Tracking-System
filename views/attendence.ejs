<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <table class="table">
        <thead>
          <tr>
            <th scope="col">Date</th>
            <th scope="col">In Time</th>
            <th scope="col">Out Time</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <%for(let i=0;i<dates.length;i++){%>
                <tr>
                <td><%=dates[i]%></td>
                <td><%=inTimes[i]%></td>
                <td><%=outTimes[i]%></td>
            </tr>
            <%}%>
          </tr>
        </tbody>
      </table>
      <form action="/attendence/<%=user.id%>" method="GET">
      <button>Return</button>
      </form>
</body>
</html>